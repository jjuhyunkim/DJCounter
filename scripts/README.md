# Scripts to run DJ counting

Set `tools` as `/path/to/DJCounter`.
For example, set `tools=/path/to/tools` in `~/.bash_profile` and clone the DJCounter repository under `$tools`.

See README files linked below:
* [Mapping based approach](mapping_based.md)
* [Reference free, K-mer based approach](kmer_based.md)
